name: CI/CD Pipeline

on:
  push:
    branches:
      - main  # Trigger on pushes to main branch
  workflow_dispatch:  # Allows manual triggering

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create .env file
        run: |
          echo "REACT_APP_app_id=${{ secrets.REACT_APP_APP_ID }}" >> server/.env
          echo "REACT_APP_app_key=${{ secrets.REACT_APP_APP_KEY }}" >> server/.env
          echo "REACT_APP_MONGO_URI=${{ secrets.REACT_APP_MONGO_URI }}" >> server/.env


      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin

      
      - name: Install Docker Compose
        run: |
          sudo apt-get update
          sudo apt-get install -y docker-compose

      - name: Build and push Docker images
        run: |
          # Build both images
          docker-compose -f docker-compose.yml build
       
          docker tag recipe-shop_web-client:latest franco32/recipe-shop-web-client:latest
          docker push franco32/recipe-shop-web-client:latest

          docker tag recipe-shop_server:latest franco32/recipe-shop-server:latest
          docker push franco32/recipe-shop-server:latest